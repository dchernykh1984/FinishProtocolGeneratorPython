[tool.poetry]
name = "finish-protocol-generator"
version = "0.1.0"
description = "Generate finish protocols for offline referee events"
authors = ["Denis Chernykh <education03@mail.ru>"]
packages = [{include = "app"}]

[tool.poetry.dependencies]
python = "^3.14"
